info:
  name: get coach List(admin)
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/coach-management/coaches/admin/list"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
