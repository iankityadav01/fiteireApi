info:
  name: get single coach
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/coach-management/coaches/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
