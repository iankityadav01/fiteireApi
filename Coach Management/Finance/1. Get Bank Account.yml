info:
  name: Get Bank Account
  type: http
  seq: 1
http:
  method: GET
  url: "{{baseUrl}}/api/v1/coach-management/bank-account/me"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
