info:
  name: Delete Weekly Slot Unavailability
  type: http
  seq: 13
http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/coach-management/availability/me/availability/slots-unavailability/:id"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
