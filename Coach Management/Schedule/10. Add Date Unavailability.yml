info:
  name: Add Date Unavailability
  type: http
  seq: 10
http:
  method: POST
  url: "{{baseUrl}}/api/v1/coach-management/availability/me/unavailability-dates"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "unavailableDate": "2026-06-16"
      }
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
