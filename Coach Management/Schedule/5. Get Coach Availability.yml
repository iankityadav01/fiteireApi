info:
  name: Get Coach Availability
  type: http
  seq: 5
http:
  method: GET
  url: "{{baseUrl}}/api/v1/coach-management/availability/coach/:coachId"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
