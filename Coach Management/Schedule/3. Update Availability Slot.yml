info:
  name: Update Availability Slot
  type: http
  seq: 3
http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/coach-management/availability/me/:id"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "startTime": "09:00",
        "endTime": "11:00",
        "isActive": true
      }
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
