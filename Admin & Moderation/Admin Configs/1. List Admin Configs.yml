info:
  name: 1. List Admin Configs
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin-moderation/admin-configs"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
