info:
  name: 2. Get Admin Config by Key
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin-moderation/admin-configs/:key"
  params:
    - name: key
      value: "chat_duration"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
