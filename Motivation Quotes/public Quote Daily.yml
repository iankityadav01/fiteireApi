info:
  name: public Quote Daily
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/v1/motivation-quotes/public/daily"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
