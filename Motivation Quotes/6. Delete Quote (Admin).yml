info:
  name: 6. Delete Quote (Admin)
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/motivation-quotes/{{quoteId}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
