info:
  name: Delete Review
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/reviews/:id"
  headers: []
  body:
    type: none
  auth: inherit
  params:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
