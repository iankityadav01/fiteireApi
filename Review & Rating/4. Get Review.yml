info:
  name: Get Review
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/reviews/:id"
  headers: []
  body:
    type: none
  auth: inherit
  params:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
