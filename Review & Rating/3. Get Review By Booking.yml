info:
  name: Get Review By Booking
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/reviews/booking/:bookingId"
  headers: []
  body:
    type: none
  auth: inherit
  params:
    - name: bookingId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
