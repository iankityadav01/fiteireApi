info:
  name: Hide Review
  type: http
  seq: 10

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/reviews/admin/reviews/:id/hide"
  headers: []
  body:
    type: none
  auth: inherit
  params:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
