info:
  name: Get Coach Rating Summary
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/v1/reviews/coaches/:coachId/summary"
  headers: []
  body:
    type: none
  auth: inherit
  params:
    - name: coachId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
