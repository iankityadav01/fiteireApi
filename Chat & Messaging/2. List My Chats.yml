info:
  name: 2. List My Chats
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/chat/chats?page=1&limit=20&status=ACTIVE"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
