info:
  name: 5. Verify Reset Code
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/verify-reset-code"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "admin@fiterio.com",
        "otp": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
