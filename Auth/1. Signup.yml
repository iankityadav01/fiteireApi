info:
  name: 1. Signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/signup"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "testuser@fiterio.com",
        "password": "Password123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
