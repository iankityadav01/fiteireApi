info:
  name: 1. Signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/signup"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "firstName": "Ankit",
        "lastName": "Yadav",
        "phone":"8901971436",
        "email": "raoankit0022@gmail.com",
        "password": "Password123!"  
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
