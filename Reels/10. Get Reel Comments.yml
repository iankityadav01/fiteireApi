info:
  name: Get Reel Comments
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/api/v1/reels/REEL_UUID_HERE/comments?page=1&limit=20"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
