info:
  name: Get Reel Feed
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/reels/feed?limit=10"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
