info:
  name: Toggle Reel Like
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/api/v1/reels/REEL_UUID_HERE/like"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
