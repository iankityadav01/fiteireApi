info:
  name: Admin Dashboard
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/api/v1/reels/admin/dashboard"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
