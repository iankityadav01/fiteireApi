info:
  name: Delete Reel
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/reels/REEL_UUID_HERE"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
