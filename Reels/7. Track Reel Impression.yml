info:
  name: Track Reel Impression
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/v1/reels/REEL_UUID_HERE/impression"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
