info:
  name: Delete Reel Comment
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/reels/comments/COMMENT_UUID_HERE"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
