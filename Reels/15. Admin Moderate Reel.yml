info:
  name: Admin Moderate Reel
  type: http
  seq: 15

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/reels/admin/REEL_UUID_HERE/status"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "status": "APPROVED"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
