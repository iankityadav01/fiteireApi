info:
  name: Get My Reels
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/reels/my-reels?page=1&limit=10&status=APPROVED&sortOrder=desc"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
