info:
  name: Admin List Reels
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/api/v1/reels/admin?page=1&limit=10&status=PENDING"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
