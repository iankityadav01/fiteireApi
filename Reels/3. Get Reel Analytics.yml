info:
  name: Get Reel Analytics
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/reels/REEL_UUID_HERE/analytics"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
