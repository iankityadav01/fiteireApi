info:
  name: 3. Get Ad By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/ad-management/ads/:id"
  params:
    - name: id
      value: "PASTE_AD_ID_HERE"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
