info:
  name: 6. Delete Ad
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/ad-management/ads/:id"
  params:
    - name: id
      value: "PASTE_AD_ID_HERE"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
