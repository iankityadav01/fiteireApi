info:
  name: 7. List Media Assets
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/v1/ad-management/ads/:adId/media"
  params:
    - name: adId
      value: "PASTE_AD_ID_HERE"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
