info:
  name: 1. Serve Ads
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/ad-management/serve"
  query:
    - name: placement
      value: HOME_TOP
    - name: platform
      value: web
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
