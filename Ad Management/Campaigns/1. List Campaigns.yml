info:
  name: 1. List Campaigns
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/ad-management/campaigns"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
