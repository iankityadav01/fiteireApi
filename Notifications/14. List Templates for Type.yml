info:
  name: 14. List Templates for Type
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/notifications/types/:typeId/templates"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
