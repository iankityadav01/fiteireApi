info:
  name: 11. List Notification Types
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/notifications/types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
