info:
  name: 3. Mark Notification as Read
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/notifications/:id/read"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
