info:
  name: 18. Cancel Batch
  type: http
  seq: 18

http:
  method: POST
  url: "{{baseUrl}}/api/v1/admin/notifications/batches/:id/cancel"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
