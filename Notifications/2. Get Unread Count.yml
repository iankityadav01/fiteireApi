info:
  name: 2. Get Unread Count
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/notifications/unread-count"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
