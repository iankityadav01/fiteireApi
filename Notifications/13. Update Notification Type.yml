info:
  name: 13. Update Notification Type
  type: http
  seq: 13

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/admin/notifications/types/:id"
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "isActive": false
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
