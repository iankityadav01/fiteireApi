info:
  name: 6. Get My Preferences
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/notifications/preferences"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
