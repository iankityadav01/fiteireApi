info:
  name: 4. Mark All as Read
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/notifications/read-all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
