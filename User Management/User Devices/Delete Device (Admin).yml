info:
  name: Delete Device (Admin)
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/user-management/devices/:id"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
