info:
  name: Get My Devices
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/me/devices"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
