info:
  name: Remove My Device
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/user-management/users/me/devices/:id"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
