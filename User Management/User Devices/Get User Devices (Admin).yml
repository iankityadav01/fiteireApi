info:
  name: Get User Devices (Admin)
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/user-management/users/:userId/devices"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
