info:
  name: Update Role
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/roles/:id"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "description": "Updated role"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
