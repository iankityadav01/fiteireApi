info:
  name: Delete Role
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/user-management/roles/:id"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
