info:
  name: List Preferences (Admin)
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/preferences"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
