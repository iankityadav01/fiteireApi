info:
  name: Get Specific User Preferences
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/:userId/preferences"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
