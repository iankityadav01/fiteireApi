info:
  name: Update My Profile
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/user-management/users/me/profile"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "firstName": "John",
        "lastName": "Doe"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
