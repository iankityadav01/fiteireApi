info:
  name: Get My Profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/me/profile"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
