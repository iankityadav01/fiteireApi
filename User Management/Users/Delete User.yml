info:
  name: Delete User
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/user-management/users/:id"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
