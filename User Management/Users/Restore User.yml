info:
  name: Restore User
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/v1/user-management/users/:id/restore"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
