info:
  name: Get Current User (/me)
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/user-management/users/me"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
