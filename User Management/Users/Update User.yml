info:
  name: Update User
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/user-management/users/:id"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "phone": "0987654321"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
