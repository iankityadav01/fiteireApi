info:
  name: Get User Refresh Tokens (Admin)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/:userId/refresh-tokens"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
