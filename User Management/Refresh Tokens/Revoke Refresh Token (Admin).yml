info:
  name: Revoke Refresh Token (Admin)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/v1/user-management/refresh-tokens/:id/revoke"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
