info:
  name: Revoke All User Refresh Tokens (Admin)
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/user-management/users/:userId/refresh-tokens/revoke-all"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
