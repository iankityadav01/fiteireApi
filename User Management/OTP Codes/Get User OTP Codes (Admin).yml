info:
  name: Get User OTP Codes (Admin)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/user-management/users/:userId/otp-codes"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
