info:
  name: Invalidate All User OTP Codes (Admin)
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/users/:userId/otp-codes/invalidate-all"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
