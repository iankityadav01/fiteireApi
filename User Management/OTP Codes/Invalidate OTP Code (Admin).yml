info:
  name: Invalidate OTP Code (Admin)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/v1/otp-codes/:id/invalidate"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
