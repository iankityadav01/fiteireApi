info:
  name: Delete OTP Code (Admin)
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/otp-codes/:id"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
