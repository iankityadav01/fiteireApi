info:
  name: Revoke My Session
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/user-management/users/me/sessions/:id/revoke"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
