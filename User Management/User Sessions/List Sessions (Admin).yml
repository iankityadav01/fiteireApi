info:
  name: List Sessions (Admin)
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/user-management/sessions"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
