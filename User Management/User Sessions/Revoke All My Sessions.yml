info:
  name: Revoke All My Sessions
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/v1/user-management/users/me/sessions/revoke-all"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
