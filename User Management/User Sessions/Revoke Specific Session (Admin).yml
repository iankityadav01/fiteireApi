info:
  name: Revoke Specific Session (Admin)
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/api/v1/user-management/sessions/:id/revoke"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
