info:
  name: Get User Sessions (Admin)
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/:userId/sessions"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
