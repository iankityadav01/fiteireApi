info:
  name: Delete Session (Admin)
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/user-management/sessions/:id"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
