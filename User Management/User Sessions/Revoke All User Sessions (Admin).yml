info:
  name: Revoke All User Sessions (Admin)
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/v1/users/:userId/sessions/revoke-all"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
