info:
  name: Create Session
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/v1/booking-and-sessions/sessions"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "bookingId": "UUID_HERE",
        "status": "SCHEDULED"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
