info:
  name: Update Session Status
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/booking-and-sessions/sessions/:id/status"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "status": "LIVE"
      }
  auth: inherit
  params:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
