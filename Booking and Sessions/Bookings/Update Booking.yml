info:
  name: Update Booking
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/booking-and-sessions/bookings/:id"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "notes": "Updated notes"
      }
  auth: inherit
  params:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
