info:
  name: List Bookings
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/booking-and-sessions/bookings"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
