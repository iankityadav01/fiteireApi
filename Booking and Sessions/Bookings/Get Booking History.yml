info:
  name: Get Booking History
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/booking-and-sessions/bookings/:id/history"
  headers: []
  body:
    type: none
  auth: inherit
  params:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
