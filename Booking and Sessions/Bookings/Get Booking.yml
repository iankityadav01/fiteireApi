info:
  name: Get Booking
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/booking-and-sessions/bookings/:id"
  headers: []
  body:
    type: none
  auth: inherit
  params:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
