info:
  name: 2. Get Payout By Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/payment-financials/payouts/:id"
  auth: inherit
  params:
    - name: id
      value: "PASTE_PAYOUT_UUID_HERE"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5