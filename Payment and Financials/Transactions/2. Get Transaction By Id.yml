info:
  name: 2. Get Transaction By Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/payment-financials/transactions/:id"
  auth: inherit
  params:
    - name: id
      value: "PASTE_TRANSACTION_UUID_HERE"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5