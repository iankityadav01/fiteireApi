info:
  name: 2. Get Wallet Transaction By Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/payment-financials/wallet-transactions/:id"
  auth: inherit
  params:
    - name: id
      value: "PASTE_WALLET_TRANSACTION_UUID_HERE"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5