info:
  name: 1. List Payments
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/payment-financials/payments?page=1&limit=10"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5