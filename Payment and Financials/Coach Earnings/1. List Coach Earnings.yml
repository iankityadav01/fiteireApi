info:
  name: 1. List Coach Earnings
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/payment-financials/coach-earnings?page=1&limit=10"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
